# Тесты

Тесты не требуют камеры, Bluetooth, Pico или YOLO.

Запуск из корня репозитория:

```bash
python3 -m unittest discover -s tests -v
```

Сейчас проверяется регулятор движения: мёртвая зона, ограничение ускорения,
ограничение PWM и аварийный сброс команды.
